# Memory Bank Layer

This folder is the durable memory layer for the service harness.

- `facts.tsv` stores reusable lessons distilled from incidents.
- `promotions.tsv` stores which lesson became a rule, why, and after which replay evidence.

Do not copy raw conversation history here. Keep this layer compact and durable.
